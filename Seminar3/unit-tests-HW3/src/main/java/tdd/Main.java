package tdd;
/*

import java.util.ArrayList;

import static tdd.UserRepository.*;

public class Main {
    public static void main(String[] args) {
        ArrayList<User> users = getUserRepo();

        addUser(new User("Светлана", "1234"));
        addUser(new User("Дмитрий", "54a"));
        addUser(new User("Олег", "555"));
        addUser(new User("Валентина", "4321"));
        addUser(new User("Геннадий", "qwe1"));
        addUser(new User("Дмитрий", "!eee!"));
        System.out.println("Добавленные в репозиторий пользователи:");
        printRepo();

        UserRepository.addAdminRights(users.get(0));
        UserRepository.addAdminRights(users.get(2));

        System.out.println("\nПользователи с обновлёнными правами:");
        printRepo();

        UserRepository.deleteNotAdmins();

        System.out.println("\nСписок пользователей после удаления всех пользователей без административных прав:");
        printRepo();
    }
}
*/
